# Chrome機能拡張 設計書

## 機能拡張名
**すごくじゃまなふせん**

---

## 1. 目的

作業中に「あと1本残っている」「これを忘れるとまずい」といった**重要だが抜けやすい未処理タスク**を、ユーザーの視界に強制的に居座らせるためのChrome機能拡張を作る。

一般的なToDoアプリやメモアプリは、見ないことができてしまう。
この拡張は逆に、**見ない自由を奪う**ことを目的とする。

コンセプトは以下の通り。

- とにかく目に入る
- かなり邪魔
- でも自分でコントロールできる
- 付箋としての軽さは保つ
- コードや設定の知識がなくても使える

---

## 2. コンセプト

### 一言でいうと
**「未処理を視界に貼りつけて逃がさないChrome拡張」**

### 体験の核
- どのページを開いても、画面上部に付箋が出る
- しかも初期設定ではかなり目立つ
- 付箋は複数枚貼れる
- 色、サイズ、透明度を変えられる
- 消すまでそこにいる

### 重要な思想
この拡張は「便利でスマートなタスク管理ツール」ではない。
むしろ少し野蛮で、**視覚的圧力で思い出させる道具**である。

「あとで見る」ではなく、
**「今、お前はまだこれを終えていない」**
をページの最前面で思い出させることが目的。

---

## 3. 想定ユーザー

- うっかり未処理を残しがちな人
- 普通のToDoアプリを見なくなる人
- 作業中に横道へ逸れやすい人
- ブラウザ上で仕事をしていて、ページ遷移が多い人
- 締切や入稿漏れなど、1件の見落としが痛い人

---

## 4. MVP（最小実装）で必要な機能

### 4.1 付箋の表示
- Chromeで開いた通常のWebページ上に付箋を表示する
- 付箋は**画面上部に固定表示**される
- スクロールしても追従する
- 常に最前面に近い見た目で表示される

### 4.2 付箋の作成
- ユーザーが任意の文章を入力して付箋を作れる
- 改行可能
- 複数枚作成できる

### 4.3 付箋の編集
- 既存の付箋の文章を編集できる
- ページ上の付箋本体からも直接編集できる
- 編集内容は即時反映または短い遅延で保存して反映する

### 4.4 付箋の削除
- 付箋ごとに削除できる

### 4.5 色の変更
- 初期色は**蛍光イエロー**
- 他の色も選べる
- MVPではプリセット選択でよい

### 4.6 サイズの変更
- 付箋ごとにサイズを変えられる
- MVPでは以下の3段階程度でよい
  - 小
  - 中
  - 大

### 4.7 透明度の変更
- 付箋ごとに透明度を変更できる
- 初期値は**90%**
- 透明度は背景に対して適用し、文字の可読性は保つ

### 4.8 表示の一時停止
- 拡張全体の表示ON/OFFを切り替えられる
- OFFにしてもデータは削除しない

### 4.9 ドラッグ移動
- 付箋をページ上でドラッグして任意の位置に移動できる
- 移動した位置は保持され、次回も同じ位置に表示される
- ウィンドウサイズが変わっても、可能な限り画面内に収める

### 4.10 データ保持
- 作成した付箋はブラウザを閉じても保持される
- 次回Chrome起動後も再表示される

### 4.11 既存タブへの即時反映
- ポップアップで作成、編集、削除、色変更、サイズ変更、透明度変更をしたら、
  すでに開いている対象タブにも自動反映される
- 反映のためにページ再読込を要求しない

---

## 5. MVPでは不要な機能（非対象）

以下は将来拡張候補だが、最初の版では不要。

- ページごと・ドメインごとの表示切り替え
- リッチテキスト編集
- 画像添付
- 共有機能
- 同期アカウント機能
- 締切日時管理
- 通知・アラーム
- カテゴリ分け・タグ管理
- 検索
- AI要約

まずは**邪魔な付箋が全ページに出る**ことを最優先にする。

---

## 6. 仕様詳細

### 6.1 表示対象ページ
- 通常の `http://` および `https://` ページに表示する
- Chrome内部ページ（例: `chrome://`）や拡張機能ページは対象外でよい

### 6.2 付箋の表示位置
初期仕様として以下を採用する。

- 画面上部固定
- 右寄せの縦積み
- 複数枚ある場合は縦に並ぶ
- 画面幅が狭い場合も崩れにくい

#### 理由
「ページの最上部に常に見える」ことが最重要。
自由配置は便利だが、MVPでは実装コストが増えるため後回し。

### 6.3 邪魔さの設計
この拡張の個性として、**適度にではなく、明確に邪魔**であることが重要。

初期設定では以下の方向にする。

- 背景色は蛍光イエロー
- 背景透明度は90%
- 文字色は黒
- やや太めのボーダー
- 軽い影を付ける
- 画面の上部にかぶさる
- やや大きめの余白
- 縮小せず、そのまま目に入るサイズ

ただし、完全に作業不能にするのではなく、
**「視界に割り込んでくるが、クリックやスクロールは普通にできる」**
程度に留める。

### 6.4 邪魔さの上限
邪魔さを維持しつつ、MVPでは以下の上限を設ける。

- 1枚あたりの幅は `220px` から `320px` の範囲に収める
- 1枚あたりの最大高さは画面高の `40%` 程度までにする
- 全体の積み上がり領域は画面高の `55%` 程度までにする
- 長文は付箋内スクロールで閲覧可能にする

### 6.5 付箋の種類
MVPではすべて**グローバル付箋**とする。
つまり、どのページを開いても同じ付箋が表示される。

#### 理由
今回の課題は「あと1本残っているのに別のことを始めてしまう」ことなので、
ページ依存メモよりも、**全ページに出る共通警告**のほうが向いている。

### 6.6 色プリセット案
初期実装ではカラーピッカーではなくプリセットでよい。

- 蛍光イエロー（初期値）
- 蛍光ピンク
- 蛍光グリーン
- オレンジ
- 水色
- 白

### 6.7 サイズプリセット案
- 小: 1行メモ向け
- 中: 通常の付箋向け（初期値）
- 大: 強制力が必要な重要メモ向け

サイズ変更対象は最低でも以下。
- 文字サイズ
- 付箋の縦横サイズ
- 内側余白

### 6.8 透明度仕様
- 透明度は付箋ごとに設定する
- 調整UIはスライダーまたは段階指定でよい
- 初期値は `90%`
- 文字色やボーダーは可読性を落としすぎないよう別扱いにしてよい

### 6.9 反映タイミング
- ポップアップでの変更は、保存後1秒以内を目安に表示中タブへ反映する
- 対象タブ側はストレージ変更を監視して再描画する

### 6.10 ドラッグ配置仕様
- 初期位置は右上の積み上げ配置とする
- ユーザーがドラッグした付箋は、その座標を優先して表示する
- 表示時に座標が画面外へ出る場合は、画面内に収まるよう補正する
- 必要なら位置リセットで初期配置へ戻せるようにしてよい

---

## 7. UI/UX設計

### 7.1 基本方針
UIはできるだけシンプルにする。
使い方に迷わせない。

必要な操作は以下だけ。
- 付箋を追加する
- 文言を変える
- 色を変える
- サイズを変える
- 透明度を変える
- 位置を動かす
- 一時的に全体表示を切り替える
- 消す

### 7.2 想定UI構成

#### A. 拡張機能ポップアップ
拡張機能アイコンを押したときに出る小さなUIで管理する。

最低限必要な要素:
- 「新しい付箋を追加」ボタン
- 新規付箋のテキスト入力欄
- 新規付箋の色選択
- 新規付箋のサイズ選択
- 新規付箋の透明度設定
- 既存付箋の一覧
- 各付箋のテキスト編集欄
- 色選択
- サイズ選択
- 透明度設定
- 表示ON/OFFのトグル
- 位置リセットボタン
- 削除ボタン
- 拡張全体の表示ON/OFFトグル

#### B. ページ上の付箋本体
ページ上には実際の付箋だけを表示する。
付箋本体上にも最低限の操作はあってよい。

MVPでは以下を採用する。
- 削除ボタン
- ドラッグ移動
- 本文の直接編集

その他は将来機能とする。
- 折りたたみボタン

MVPでは、細かい管理はポップアップ側に寄せる。

---

## 8. ユースケース

### ユースケース1
#### 状況
「SDAAあと1本」などの未処理がある状態で、つい別のサイトや配信準備に脱線してしまう。

#### 使い方
- 拡張を開く
- 「SDAAあと1本」付箋を作る
- 以後、どのページを開いても上部に表示され続ける
- 終わったら削除する

### ユースケース2
#### 状況
複数の締切や用件が混在している

#### 使い方
- 複数枚の付箋を作る
- 重要度に応じて色、サイズ、透明度を変える
- 一番重要なものを大サイズ、蛍光イエロー、高不透明で表示する

### ユースケース3
#### 状況
普段のメモではなく、今日は絶対に忘れたくないことだけを目立たせたい

#### 使い方
- 一時的に1枚だけ作る
- 終わったら消す
- 常設ToDoではなく、**緊急の視覚トリガー**として使う

---

## 9. データ設計（概念レベル）

### 9.1 保存単位
保存データは以下の形を基本とする。

- `schemaVersion`
- `enabled`（拡張全体の表示ON/OFF）
- `notes`（付箋配列）

### 9.2 各付箋の項目
各付箋は以下の情報を持つ。

- `id`
- `text`
- `color`
- `size`
- `opacity`
- `position`
- `createdAt`
- `updatedAt`
- `visible`（表示/非表示）
- `order`（並び順。MVPでは作成順ベースでよい）

### 9.3 データの保存先
- Chrome拡張のローカル保存機能を使う
- 初期段階ではローカル保存で十分
- 同期は必須ではない

---

## 10. 実装方針（Codex向け）

### 10.1 拡張形式
- Chrome Extension
- Manifest V3 前提

### 10.2 想定構成
最低限以下の構成を想定。

- `manifest.json`
- `popup.html`
- `popup.js`
- `content.js`
- `shared.js`
- `styles.css`

### 10.3 役割分担
- **popup側**: 付箋の追加、編集、削除、色変更、サイズ変更、透明度変更、全体表示切替
- **content script側**: 各ページに付箋を差し込んで描画
- **storage側**: 付箋データの保存、読み込み、変更通知

### 10.4 レンダリング方針
- content script がページ読み込み時に付箋コンテナを挿入する
- 保存済みの付箋一覧を読み込んでDOM生成する
- z-index は高めに設定する
- 初期配置は右上の縦積みとし、ドラッグ後は保存座標で表示する
- 長文時は付箋内部または一覧全体をスクロール可能にする

### 10.5 競合対策
- サイト固有のCSSに潰されにくいようにする
- class名やid名は衝突しにくいプレフィックスを付ける
- MVPでも **Shadow DOM を第一候補** とする

### 10.6 即時同期
- popup側の保存後、`chrome.storage` の変更通知で各タブを再描画する
- 手動リロードを前提にしない

---

## 11. 邪魔さのチューニング指針

この拡張の価値は「控えめさ」ではない。
ただし、ただ不快なだけでもだめ。

以下のバランスを取る。

### 欲しい状態
- 視界に入る
- 忘れにくい
- でも作業続行は可能

### 避けたい状態
- ページを完全に読めない
- ボタン操作を妨害しすぎる
- 毎回すぐ消したくなる
- うるさすぎて常用不能

### 初期値の考え方
初期値は**「ちょっとやりすぎ」寄り**でよい。
ユーザーが必要に応じて色、サイズ、透明度を落とせるようにする。

理由:
この拡張を入れるユーザーは、そもそも「普通の目立ち方」では足りない可能性が高いから。

---

## 12. アクセシビリティ・使い勝手

最低限以下に配慮する。

- 文字が読めるコントラスト
- 小さすぎない文字サイズ
- ボタンを誤操作しにくいサイズ
- 改行を含むメモに対応
- 文字数が増えても見切れすぎない
- 透明度を下げても文字が読める

ただし、洗練されたミニマルUIより、**使っていて忘れにくいこと**を優先する。

---

## 13. 将来拡張候補

MVP後に検討できる機能。

### 13.1 サイト別表示
- 特定サイトだけで表示
- 特定サイトでは非表示

### 13.2 優先度設定
- 高優先度は赤系
- 低優先度は淡色

### 13.3 一時折りたたみ
- 完全削除ではなく一時的に畳む

### 13.4 期限付き付箋
- 今日だけ表示
- 指定時刻を過ぎたら消える

### 13.5 テンプレート付箋
- 「あと1本」
- 「入稿前チェック」
- 「17時まで」
などを定型として登録

### 13.6 強制モード
- 画面上部帯表示
- 背景点滅（やりすぎ注意）
- 一定間隔で強調アニメーション

---

## 14. 成功条件

この拡張が成功したと言える条件は以下。

- ユーザーが数分で使い始められる
- 付箋を1枚作れば、全ページで目に入る
- 重要な未処理を忘れにくくなる
- 「邪魔だけど役に立つ」と感じられる
- ToDoアプリを見なくなるタイプの人にも効く

---

## 15. 受け入れ基準（Acceptance Criteria）

MVP完成時に満たしてほしい条件。

1. Chrome拡張としてインストールできる
2. ポップアップから付箋を新規作成できる
3. 作成した付箋が通常のWebページ上部に固定表示される
4. 付箋は複数枚作成できる
5. 付箋ごとに色を変更できる
6. 付箋ごとにサイズを変更できる
7. 付箋ごとに透明度を変更できる
8. 付箋の文言を編集できる
9. ページ上の付箋本体からも文言を編集できる
10. 付箋を削除できる
11. 付箋をドラッグで任意位置へ移動できる
12. 移動した位置が保持される
13. 拡張全体の表示をON/OFFできる
14. ブラウザ再起動後も付箋が保持される
15. 初期色が蛍光イエローである
16. 初期透明度が90%である
17. ポップアップでの変更が既存タブにも再読込なしで反映される
18. 一般的なWebサイト上で十分目立つ
19. サイト閲覧や基本的なクリック操作は可能
20. 320px幅前後の狭い表示でも大きく崩れない

---

## 16. Codexへの補足メモ

- コードの美しさより、まずは体験の成立を優先してほしい
- UIは凝らなくていいが、**“すごくじゃま”感**はちゃんと出してほしい
- 最初の版は、できるだけ構成を単純に保つ
- 実装後は、実際に複数サイトで表示して、邪魔さと崩れ具合を確認してほしい
- 名前にふさわしく、「控えめで上品」ではなく「明確に視界へ割り込む」挙動を優先してほしい
